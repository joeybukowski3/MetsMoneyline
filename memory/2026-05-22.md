# 2026-05-22

- Diagnosed the `rebuild-depth-chart` GitHub Actions failure at `Commit refreshed depth chart artifacts` as a workflow ordering problem, not a depth chart generator bug.
- The job was committing `public/data/depth-chart.json` and `public/sitemap.xml`, then running `git pull --rebase origin main` afterward.
- That pattern is brittle because `public/sitemap.xml` is a shared generated artifact that other automation can update between checkout and push, creating a post-commit rebase conflict inside the same step.
- Fixed `.github/workflows/depth-chart-update.yml` by rebasing onto `origin/main` immediately after checkout, before any artifact generation, and by removing the post-commit `git pull --rebase` / extra `git add -A`.
- Kept the workflow behavior otherwise the same: rebuild depth chart data, regenerate sitemap, commit only the intended artifacts, and push to `main`.
- Fixed the schedule page in `public/schedule.html` to use the shared `<header>` / `<footer>` mount points and the shared `site-footer.js` renderer, replacing the nonstandard placeholder div roots that prevented the page from matching the rest of the site layout.
- Removed the desktop fixed-height inner scroll behavior from both schedule tables so all rows render inline on desktop, while preserving a mobile-only capped scroll fallback for the tabbed single-column view.
- Reworked the schedule hero summaries to derive played/upcoming opponent metrics directly from `public/data/game-log.json` row data instead of the pre-rounded `summary.sosPlayed` / `summary.sosUpcoming` snapshot, which had collapsed different opponent batting averages into the same displayed `.240`.
- Added collective opponent record and collective winning percentage to both the `Opponents Faced` and `Opponents Remaining` sections using the same played/upcoming row datasets as the other displayed schedule metrics.
- Updated the schedule summary cards again in `public/schedule.html`: removed `Collective W%`, renamed `Avg Opp W%` to `Opponent Win %`, added Mets home/away record to the faced panel, and added remaining home/away game counts plus remaining home/away opponent win % to the remaining panel.
- Wired `Average Power Ranking` into both schedule summary panels using `public/data/power-rankings.json`, matching opponents by `oppTeamId` to the rankings page's `teamId` keys and averaging ranks across the same played/upcoming row sets used for the rest of the summary metrics.
- Adjusted the summary stat layout from a flex row to a small responsive grid so the larger stat set still fits the hero card cleanly on desktop and mobile without changing the overall page structure.
- Updated `public/game-log.html` so the top analytics tiles use full-text labels on larger layouts (`Average Mets Runs`, `Average Opponent Runs`, `Average Mets Hits`, `Average Opponent Hits`, `Win Percentage`) and automatically fall back to the previous abbreviated labels on smaller screens.
- Fixed the missing opponent road record source bug in `bot/build-game-log.js`: the standings split lookup was only checking for `road`, while the MLB standings payload available to this script resolves the away split under `away`, leaving every generated `oppRoadRecord` null.
- Rebuilt `public/data/game-log.json` after the generator fix and verified that `oppRoadRecord` now populates correctly in the generated rows (for example Pirates `13-11`, Cardinals `15-8`, Giants `10-18`).
- Diagnosed the site-wide odds outage as a shared cache-fallback failure: `bot/build-api-cache.js` wrote an empty `public/api/mlb/mets/odds.json` whenever live providers returned no usable markets, even though same-day archived Mets lines already existed in `public/data/odds-history.json`.
- Added a reusable archived-odds fallback helper in `lib/archived-odds-fallback.js` and wired it into both `bot/build-api-cache.js` and `api/_lib/mlb-data.js` so the static odds cache and live odds handler can rebuild a minimal `h2h` moneyline payload from same-day archived history when live odds are unavailable.
- Prevented fallback loops by skipping odds-history re-archival inside `bot/build-api-cache.js` when the current payload was itself rebuilt from archived history.
- Tightened consumer fallback behavior after the shared payload fix: `public/js/main.js` now clears stale opponent prices when a current odds payload only has the Mets side, and `public/js/report-live-odds.js` now overwrites the report odds row with `N/A` for any missing side instead of leaving an older stale value visible.
- Tightened `bot/generator.js` moneyline parsing to use exact team-name matching and the correct home/away-based opponent fallback, so sparse moneyline outcomes no longer get matched back to the Mets side by accident during report/sample-game generation.
- Updated the shared report/table percentile color mapping in `bot/generator.js` from coarse three-band snapping (blue / gray / orange) to a smoother interpolated gradient around a neutral midpoint. The new mapping keeps the existing percentile and stat-direction rules but reduces flat gray cells by tinting values progressively as they move away from league average.
- Added a local sportsbook asset set under `public/assets/sportsbooks/` and a shared bookmaker/logo manifest in `public/js/sportsbook-branding.js`. The current live `public/api/mlb/mets/odds.json` only exposes `archived-consensus` / `Oddschecker Best`, so the manifest explicitly leaves that entry in text-fallback mode while providing normalized aliases and local assets for the betting page's tracked books: Fanatics, DraftKings, FanDuel, BetMGM, and Caesars.
- Wired the betting page to that shared branding manifest by loading `public/js/sportsbook-branding.js` before `public/js/betting.js`, replacing the hardcoded inline sportsbook SVG block in `betting.js`, and rendering manifest-driven local logos for known books in the badge row, sportsbook table/card labels, and prop price pills. Unknown or fallback-only books now render as clean text without broken image placeholders.
- Fixed the depth-chart headshot helper in `public/js/depth-chart-live-voting.js` so direct `upload.wikimedia.org/wikipedia/commons/...` profile-image URLs are converted into Wikimedia thumbnail URLs instead of being used raw. This fixes missing legacy-player images such as Tom Seaver and improves reliability for other depth-chart entries that use direct Commons file links.
