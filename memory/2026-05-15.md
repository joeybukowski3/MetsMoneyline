# 2026-05-15

- Diagnosed the live odds outage as a workflow/runtime resilience problem, not just a frontend rendering bug.
- Confirmed `public/js/main.js` homepage moneyline display reads the generated static cache `public/api/mlb/mets/odds.json`, while `public/js/betting.js` prefers the live `/api/mlb/mets/odds` handler and then falls back to the static cache.
- Confirmed `bot/generator.js` also consumes `public/api/mlb/mets/odds.json`, so stale or empty cache data propagates into `public/data/sample-game.json` and `public/report.html`.
- Updated `bot/build-api-cache.js` so cache generation no longer hard-fails when `API_SPORTS_KEY` is missing; it now continues with MLB Stats fallback for the upcoming game and preserves standings/odds generation flow instead of aborting the whole refresh.
- Hardened `bot/build-api-cache.js` metadata handling so preserved odds keep the correct provider metadata and missing standings do not crash the generated payload.
- Added a manual May 14, 2026 Detroit Tigers backfill line (`-164`) to `bot/refresh-pick-history.js` so the moneyline history generator can fully grade the game.
- Regenerated `public/data/pick-history.json`; the 2026-05-14 Tigers game is now graded as a win at `-164`, `missingOddsGames` is now `0`, and total season profit recalculated to `-1090.27`.
- Rebuilt the game package with `node bot/generator.js`; `public/data/sample-game.json` and `public/report.html` now reflect today’s Mets/Yankees moneyline at Mets `-164`, Yankees `+138`.
- Because local odds API credentials were unavailable, applied a one-off manual refresh to `public/api/mlb/mets/odds.json` using current external market listings so the live static site has active prices for today while the source-level cache-builder fix prevents future hard failures.
- Diagnosed the `/social` page as a source-level `bot/` + `public/` workflow issue, not a one-off bad JSON artifact. The page is powered by `public/social-pulse.html` + `public/js/social-pulse.js`, which reads generated data from `public/data/social-pulse.json`, and that artifact is written by `bot/update-social-pulse.js` via `.github/workflows/social-pulse-update.yml`.
- Reworked social-player ownership so the updater now derives `currentPlayers` from the live MLB active Mets roster and keeps curated ex-Mets in `bot/social-player-config.js` for separate `formerPlayers` tracking.
- Tightened social relevance filtering to avoid broad substring matches like bare `"mets"` and topic aliases like `"il"` from polluting the feed with unrelated posts.
- Updated the social payload schema so `currentPlayers` and `formerPlayers` each carry recent post detail, while the main `overallScore`, `trendingTopics`, source breakdown, and main `posts` feed are based only on current-team/team-context discussion.
- Updated the `/social` page UI so current-player names and former-player names are clickable and open a dedicated player-post detail panel with text, platform, timestamp, and source link when available.
