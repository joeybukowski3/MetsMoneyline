# 2026-05-14

- Diagnosed homepage `"No game today"` / stale matchup behavior as a workflow ownership problem rather than a frontend date-selection bug.
- Confirmed `.github/workflows/daily-update.yml` previously ran on `0 14,22 * * *`, refreshed odds/trends/prospects only, and never called `bot/generator.js` or committed `public/data/sample-game.json`.
- Confirmed `bot/generator.js` already resolves the next upcoming Mets game when no exact same-day game exists by querying an MLB Stats API date window with `allowFutureFallback: true`.
- Confirmed homepage featured-game logic already supports off-day fallback to `"Tomorrow's Game"` / `"Next Game"` using `public/js/featured-game-state.js`; no frontend source change was needed for this fix.
- Updated `.github/workflows/daily-update.yml` to run daily at `0 6 * * *` (`2:00 AM ET`), execute `node bot/generator.js`, and commit `public/data/sample-game.json` plus `public/report.html` along with the other site data artifacts.
- Ran `node bot/generator.js` locally on 2026-05-14 and regenerated `public/data/sample-game.json`, `public/report.html`, `public/rss.xml`, and `public/sitemap.xml` for the 2026-05-14 Mets vs. Tigers game.
- Audited sitewide stat color logic across `bot/`, `public/js/`, and page-level UI helpers to normalize the semantic mapping to green = positive, gray = neutral, red = negative.
- Fixed reversed shared palettes in `public/css/styles.css`, `public/js/main.js`, and `public/js/advanced-stats.js` so Statcast chips, advanced matchup bars, and advanced-stats table cells no longer treat strong metrics as red or weak metrics as blue.
- Fixed `bot/generator.js` report rendering so pitcher-only labels and batter-only labels are evaluated with context-specific percentile rules; batter `K%`, `BB%`, `Hard Hit %`, and `Barrel %` no longer inherit pitcher logic in mixed matchup tables or projected lineup comparison rows.
- Regenerated the report with `node bot/generator.js --date 2026-05-13` and verified corrected examples in `public/report.html`, including pitcher `xBAAllowed 0.192` / `K% 32.6%` showing green and batter `hardHitPct 29.5%` / high strikeout rows showing red where appropriate.
- Fixed the recurring homepage `"No game today"` regression at the source. `public/js/featured-game-state.js` now uses a 2:00 AM ET baseball-day rollover and prefers live, same-day playable, then next-upcoming games before any final fallback.
- Updated `public/js/main.js` to use the 2:00 AM ET reference date for cache scoping and to fetch a direct MLB schedule fallback whenever cached/home endpoint data has no playable current-or-next game.
- Updated `bot/generator.js` to prefer external MLB schedule data over recycled local cached game context, skip exact-date final games when future fallback is allowed, and choose the next playable Mets game from a 7-day schedule window.
- Updated `.github/workflows/daily-update.yml` from once daily to every 4 hours so `public/data/sample-game.json` refreshes throughout the day instead of only at 2:00 AM ET.
- Regenerated `public/data/sample-game.json` and `public/report.html`; the sample-game payload now points to the 2026-05-15 Yankees matchup with `canonicalGameSource.source = "external/mlb-stats"` instead of stale local context.
