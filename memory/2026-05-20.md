# 2026-05-20

- Traced the daily report send path in `bot/check-and-send-report.js` and confirmed the delivered Buttondown email was being downgraded on the final `PATCH`: draft creation used HTML, but the update step still wrote legacy plain text into `body`.
- Added `buildDailyReportEmailHtml(game)` in `bot/generator.js` as the single source of truth for the daily email document. It now returns a full HTML email with a hidden preheader, Gmail-safe inline styling, and renamed sections for the designed matchup snapshot format.
- Updated Buttondown subject generation to the new format: `MetsMoneyline: [Pick] vs [Opponent] — [Confidence] Confidence Matchup Snapshot`.
- Updated Buttondown payload generation to force rich-editor HTML via `<!-- buttondown-editor-mode: fancy -->` and to send the designed HTML through the canonical `body` field instead of relying on the legacy plain-text fallback.
- Updated `bot/check-and-send-report.js` to write `bot/output/latest-email-preview.html` before send/update and log body length plus section-presence checks for `Matchup Snapshot`, `Last 20 Game Trend`, and `Bullpen Trend`.
- Verified locally with `node -c bot/check-and-send-report.js` and a local render from `public/data/sample-game.json`; the preview file was created and the rendered HTML contained the expected designed sections and CTA.
- Updated `.github/workflows/report-send.yml` so `workflow_dispatch` now exposes separate `force_draft` and `force_send` inputs. The workflow fails fast if both are true, maps `force_draft` to `--draft-only`, and maps `force_send` to `--force-send`.
- Added `--draft-only` and `--force-send` handling in `bot/check-and-send-report.js`. Draft-only mode now stops after creating/updating a Buttondown draft in `status: "draft"` and never advances to `about_to_send`; force-send remains the explicit live-send override.
- Strengthened pre-send logging in `bot/check-and-send-report.js` to report mode, subject, target Buttondown status, HTML length, section checks, and whether the legacy `TONIGHT'S PICK` marker leaked into the final HTML body.
- Local `node check-and-send-report.js --skip-window --allow-duplicate --draft-only` confirmed the new mode selection and preview generation, but the run could not create a live Buttondown draft from this machine because `BUTTONDOWN_API_KEY` is not set locally.
