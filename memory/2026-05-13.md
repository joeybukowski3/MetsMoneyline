# 2026-05-13

- Enhanced the betting page to keep the main game table sportsbook-based while rendering player props as top-3 best-price lists instead of sparse sportsbook columns.
- Updated `public/js/betting.js` to prefer the live `/api/mlb/mets/odds` payload, fall back to the generated `api/mlb/mets/odds.json` artifact, and gate raw-response debugging behind `?debugOdds=1`.
- Expanded the game table UI in `public/betting.html` with prop-table and best-line styling that stays compact on mobile.
- Extended `api/_lib/mlb-data.js` so the live odds payload can fetch The Odds API game lines plus `player_strikeouts` and `player_home_runs`, while falling back cleanly if `API_SPORTS_KEY` is missing.
- Verified from the deployed `odds.json` artifact that on 2026-05-13 the current feed includes FanDuel, DraftKings, and BetMGM but not Fanatics or Caesars, so those blank game-table cells are source-data gaps rather than a frontend parsing bug.
- Found an important deployment behavior: `/api/mlb/mets/next-game` currently hits the live handler and returned `Missing API_SPORTS_KEY` in production testing, so the betting page should not depend on that route as a static frontend feed.
