# 2026-05-21

- Diagnosed the broken `/news` page as a source-of-truth split: `public/news.html` was still fetching third-party RSS through `https://api.rss2json.com/...`, while the working home page sidebar already used the first-party `/api/news` proxy.
- Confirmed `vercel.json` routing was not the issue. `/news` already rewrites to `public/news.html`, and `/news.html` redirects to `/news`.
- Replaced the dedicated news page’s inline proxy-based fetch logic with `public/js/news-page.js`, which now loads `/api/news?limit=24`, renders cards defensively, includes descriptions, and shows a true empty state only when no valid articles remain.
- Added `public/js/news-feed.js` as shared client-side news helpers for relative time, safe external URLs, and article normalization so `/news` and the home page sidebar use the same field handling.
- Updated the home page sidebar script in `public/index.html` to use the shared news helpers while preserving the existing `/api/news` source and sidebar layout.
- Extended `api/news.js` with a capped `limit` query parameter and article deduping so the dedicated page can request more items without changing the home page’s default payload size.
- Verified locally with `node --check api/news.js`, `node --check public/js/news-feed.js`, and `node --check public/js/news-page.js`.
- Verified end to end with `vercel dev` plus headless Edge against `http://127.0.0.1:4123/`, `http://127.0.0.1:4123/news`, and `http://127.0.0.1:4123/api/news?limit=12`; the home page sidebar rendered news cards and `/news` rendered full article cards from the shared API source.
