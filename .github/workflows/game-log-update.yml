name: Game Log Update

on:
  schedule:
    - cron: "0 8 * * *"   # 3 AM ET daily (late games always finished by then)
  workflow_dispatch:         # manual trigger

permissions:
  contents: write

jobs:
  update-game-log:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: bot/package-lock.json

      - name: Install bot dependencies
        working-directory: ./bot
        run: npm ci

      - name: Build game log
        working-directory: ./bot
        run: node build-game-log.js

      - name: Commit and push game-log.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/game-log.json
          if git diff --staged --quiet; then
            echo "No game log changes to commit."
            exit 0
          fi
          git commit -m "chore: refresh game-log.json [skip ci]"
          git add -A || true
          git pull --rebase -X theirs origin main || (git rebase --abort; git fetch origin main; git pull --rebase -X theirs origin main)
          git push origin main
